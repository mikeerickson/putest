#behat.yml
default:
    suites:
        Listings:
            paths:  [ %paths.base%/app/tests/features/listings ]
            contexts:
                - ListingsContext:
                    baseUrl: http://http://localhost:8000
